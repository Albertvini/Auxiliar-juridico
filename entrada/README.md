# entrada/

Coloque aqui os PDFs (ou textos copiados) das intimações baixadas do PROJUDI e rode `/triagem`.
O agente identifica o processo de cada arquivo, move-o para `processos/<número>/autos/`, analisa tudo e monta o
painel de prazos. Nada nesta pasta é versionado.
